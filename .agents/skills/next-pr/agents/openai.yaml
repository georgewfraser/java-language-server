interface:
  display_name: "Next PR"
  short_description: "Advance the easiest PRs in sequence."
  default_prompt: "Use $next-pr to work through the easiest PRs in order, reuse the just-validated baseline between consecutive cycles when nothing changed, and stop for approval before each merge."
